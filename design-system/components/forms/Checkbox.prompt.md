Square checkbox. Controlled via `checked` + `onChange(next)`; supports `indeterminate`.

```jsx
<Checkbox checked={agree} onChange={setAgree} label="I agree to the terms" />
<Checkbox indeterminate label="Select all" />
```
