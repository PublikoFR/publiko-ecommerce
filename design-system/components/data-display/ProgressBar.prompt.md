Linear progress / meter.

```jsx
<ProgressBar value={68} showLabel label="Season completion" />
<ProgressBar value={3} max={5} tone="forest" size="sm" />
```

Tones: `lime` `forest` `success` `warning` `danger`.
