Inline banner for status messages, with a left accent bar.

```jsx
<Alert tone="success" title="Saved" onClose={dismiss}>Your changes are live.</Alert>
<Alert tone="warning" title="Low moisture detected" />
<Alert tone="brand">Tip: schedule irrigation before noon.</Alert>
```

Tones: `info` `success` `warning` `danger` `brand`.
