@props(['statePath' => null])

<x-filament-tiptap-editor::button
    action="window.pkoProductEditor && window.pkoProductEditor.insertFromMedia()"
    label="Insérer une image (médiathèque ou URL)"
    icon="media"
/>
