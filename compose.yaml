services:
    app:
        build:
            context: ./docker/app
            dockerfile: Dockerfile
            args:
                UID: ${UID:-1000}
                GID: ${GID:-1000}
        container_name: weklo-app
        restart: unless-stopped
        working_dir: /var/www/html
        volumes:
            - .:/var/www/html
            - ./docker/app/php.ini:/usr/local/etc/php/conf.d/zz-weklo.ini:ro
        environment:
            APP_ENV: ${APP_ENV:-local}
            APP_DEBUG: ${APP_DEBUG:-true}
            PHP_IDE_CONFIG: serverName=weklo
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_started
        networks:
            - backend
            - traefik_network
        labels:
            - traefik.enable=true
            - traefik.docker.network=traefik_network
            - traefik.http.routers.weklo.rule=Host(`weklo.localhost`)
            - traefik.http.routers.weklo.entrypoints=web
            - traefik.http.services.weklo.loadbalancer.server.port=80

    # Consommateur de la file (QUEUE_CONNECTION=redis). Sans lui, tout job dispatché
    # s'empile indéfiniment : la création d'étiquette transporteur, les conversions
    # média et les e-mails ne s'exécutent jamais, sans la moindre erreur visible.
    queue:
        build:
            context: ./docker/app
            dockerfile: Dockerfile
            args:
                UID: ${UID:-1000}
                GID: ${GID:-1000}
        container_name: weklo-queue
        restart: unless-stopped
        working_dir: /var/www/html
        user: sail
        volumes:
            - .:/var/www/html
            - ./docker/app/php.ini:/usr/local/etc/php/conf.d/zz-weklo.ini:ro
        environment:
            APP_ENV: ${APP_ENV:-local}
            APP_DEBUG: ${APP_DEBUG:-true}
        # --tries/--backoff laissés aux jobs, qui définissent déjà leur politique
        # (CreateCarrierShipmentJob : 5 tentatives, backoff jusqu'à 4 h).
        command: php artisan queue:work --timeout=120 --sleep=3 --max-time=3600
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_started
        networks:
            - backend

    mysql:
        image: mysql:8.0
        container_name: weklo-mysql
        restart: unless-stopped
        command: --default-authentication-plugin=caching_sha2_password
        environment:
            MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root_password}
            MYSQL_DATABASE: ${DB_DATABASE:-weklo}
            MYSQL_USER: ${DB_USERNAME:-weklo}
            MYSQL_PASSWORD: ${DB_PASSWORD:-app_password}
        volumes:
            - mysql_data:/var/lib/mysql
            - ./docker/mysql/my.cnf:/etc/mysql/conf.d/zz-weklo.cnf:ro
        networks:
            - backend
        healthcheck:
            test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost', '-u', 'root', '-p${DB_ROOT_PASSWORD:-root_password}']
            interval: 10s
            timeout: 5s
            retries: 5

    redis:
        image: redis:7-alpine
        container_name: weklo-redis
        restart: unless-stopped
        volumes:
            - redis_data:/data
        networks:
            - backend

    phpmyadmin:
        image: phpmyadmin:latest
        container_name: weklo-pma
        restart: unless-stopped
        environment:
            PMA_HOST: mysql
            PMA_USER: root
            PMA_PASSWORD: ${DB_ROOT_PASSWORD:-root_password}
            UPLOAD_LIMIT: 256M
        depends_on:
            mysql:
                condition: service_healthy
        networks:
            - backend
            - traefik_network
        labels:
            - traefik.enable=true
            - traefik.docker.network=traefik_network
            - traefik.http.routers.weklo-pma.rule=Host(`pma.weklo.localhost`)
            - traefik.http.routers.weklo-pma.entrypoints=web
            - traefik.http.services.weklo-pma.loadbalancer.server.port=80

networks:
    backend:
        driver: bridge
    traefik_network:
        external: true

volumes:
    mysql_data:
        driver: local
    redis_data:
        driver: local
