@AGENTS.md

Les instructions de ce dépôt vivent dans AGENTS.md — fichier unique, lu par
tous les agents : Codex, Cursor, Grok et Zed le trouvent nativement, Claude Code
le reçoit par l'import ci-dessus. Si cet import n'a pas été résolu, lis
AGENTS.md à la racine avant toute chose.

Ne rien écrire ici : une règle ajoutée dans ce fichier serait invisible de la
moitié des outils.
